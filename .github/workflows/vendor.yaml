name: Vendor

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write

jobs:
  vendir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install just
        uses: extractions/setup-just@v3

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "stable"

      - name: Install vendir
        run: just ci-setup-vendir

      - name: Check vendored assets are up to date
        run: |
          just vendor-check

  vendir_autofix_renovate:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.actor == 'renovate[bot]'
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Install just
        uses: extractions/setup-just@v3

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "stable"

      - name: Install vendir
        run: just ci-setup-vendir

      - name: Sync vendored assets
        run: |
          just vendor-sync

      - name: Commit and push if needed
        run: |
          if git diff --quiet -- vendir.lock.yml stricknani/static/vendor
          then
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add vendir.lock.yml stricknani/static/vendor
          git commit -m "chore(vendor): vendir sync"
          git push
