{
  "name": "stricknani-tiptap-vendor",
  "private": true,
  "type": "module",
  "description": "Build-only input for `just vendor-tiptap`: bundles TipTap into a single self-contained ESM file (stricknani/static/vendor/tiptap/tiptap-bundle.js) so the editor never loads modules from a CDN. Not shipped or imported directly -- see entry.js and the justfile recipe.",
  "devDependencies": {
    "@tiptap/core": "3.19.0",
    "@tiptap/extension-image": "3.19.0",
    "@tiptap/extension-link": "3.19.0",
    "@tiptap/extension-underline": "3.19.0",
    "@tiptap/markdown": "3.19.0",
    "@tiptap/pm": "3.19.0",
    "@tiptap/starter-kit": "3.19.0",
    "esbuild": "0.28.2"
  },
  "scripts": {
    "build": "esbuild entry.js --bundle --format=esm --minify --outfile=tiptap-bundle.min.js"
  }
}
